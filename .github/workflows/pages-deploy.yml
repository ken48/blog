name: Build and Deploy
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: site
    steps:
      - name: Check out site
        uses: actions/checkout@v7
      - name: Check out notes
        uses: actions/checkout@v7
        with:
          repository: ken48/obsidian-posts
          path: notes-source
          persist-credentials: false
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.4'
          bundler-cache: true
          working-directory: site
      - name: Prepare articles
        run: python3 ../tools/prepare-articles.py
      - name: Prepare Markdown notes
        run: ruby ../tools/prepare-posts.rb ../notes-source
      - name: Build site
        run: bundle exec jekyll build -d _site/blog
        env:
          JEKYLL_ENV: production
      - name: Check internal links
        run: bundle exec htmlproofer _site --disable-external
      - uses: actions/upload-pages-artifact@v5
        if: github.event_name != 'pull_request'
        with:
          path: site/_site/blog

  deploy:
    if: github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/deploy-pages@v5
        id: deployment
